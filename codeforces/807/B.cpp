#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin>>n;
        vector<int> rooms(n);
        int sum = 0, min = INT_MAX;
        for(int i=0; i<n-1; i++) cin>>rooms[i];
        
    }
}